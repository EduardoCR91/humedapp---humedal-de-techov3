<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:background">@null</item>
    </style>


    <!-- Tema de lanzamiento / splash -->
    <!-- Usamos Theme.SplashScreen (Android 12+) y además android:windowBackground como fallback para versiones anteriores -->
    <style name="AppTheme.NoActionBarLaunch" parent="Theme.SplashScreen">
        <!-- Android 12+ -->
        <item name="windowSplashScreenBackground">@drawable/splash</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/splash</item>
        <item name="postSplashScreenTheme">@style/AppTheme.NoActionBar</item>

        <!-- Fallback para Android < 12: usa la misma imagen como fondo de ventana -->
        <item name="android:windowBackground">@drawable/splash</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowFullscreen">true</item>
    </style>

    <!-- NUEVO: Tema específico para el Splash Screen -->
    <style name="Theme.App.Starting" parent="Theme.SplashScreen">
        <!-- El fondo azul (ajusta el código hexadecimal al de tu icono) -->
        <item name="windowSplashScreenBackground">#0000FF</item>

        <!-- Tu imagen splash.png -->
        <item name="windowSplashScreenAnimatedIcon">@drawable/splash</item>

        <!-- El tema al que cambiará la app automáticamente después del splash -->
        <item name="postSplashScreenTheme">@style/AppTheme.NoActionBar</item>
    </style>

<!-- Usa el parent de la librería que tienes en tus dependencias -->
    <!-- <style name="AppTheme.NoActionBarLaunch" parent="Theme.SplashScreen">
        <!- 1. El fondo azul que querías (sustituye por tu color azul) -->
        
    <!-- Tema normal de la app (el que ya tienes) -->
    <!-- <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style> -->
</resources>
